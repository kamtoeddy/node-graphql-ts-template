import { UserInput } from '@/types'

export { addUser }

// eslint-disable-next-line no-unused-vars
const addUser = async (values: Partial<UserInput>) => {
  return { data: 'Work in progress!', success: false }
}
